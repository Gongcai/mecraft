# WARNING

## 编译测试

- Windows平台参考build.ps1
- Linux平台参考build.sh  

## 语言使用

- 代码注释必须使用英文，且必须清晰描述函数目的、参数意义、返回值含义、算法思路等关键信息。禁止使用中文注释或不规范的缩写。
- 给用户的汇报和文档必须使用中文。

## 代码规范

- 代码规范须符合C++17标准，多使用现代C++特性
- 禁止架构或设计层面的特例回退（fallback）。出现缺陷必须修复根因：例如用 int8 存储角度导致大于 255 度的数据运行时回绕，正确做法是将存储类型改为 int16，而不是为大于 255 度的情况添加特例处理。这是一条严格执行的规则。
- 允许资源缺失类兜底，但必须显式上报：例如方块贴图缺失时使用默认贴图、音频缺失时在控制台输出缺失信息。兜底只能掩盖单个资源的缺失，不得掩盖逻辑或架构错误。
- 本项目需要高性能开发，严禁使用try catch等异常捕获方法，使用std::optinal或错误码进行处理

## Git提交

- 提交消息使用<type>(<scope>): <subject>格式，subject使用中文，type和scope使用英文
- 每完成一轮工作，提交代码及相关修改
